# SRSFlashCardMan.github.io
A lightweight, single-file web application for Spaced Repetition System (SRS) flashcards. It implements the SRS algorithm to optimize review dates, storing all data (cards, schedule, stats) locally and securely via IndexedDB. The app supports Markdown content and is ideal for free, static hosting like GitHub Pages.
